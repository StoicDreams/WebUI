[package]
name = "webapp"
version = "0.10.11" #syncwebui
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
webui = { path = "../webui", features = [] }
serde = { version = "1.0", features = ["derive"] }

[package]
name = "webapp"
version = "0.11.4" #syncwebui
edition = "2024"

[lib]
crate-type = ["cdylib"] # Critical for wasm libraries

[dependencies]
webui = { path = "../webui", features = [] }
serde = { version = "1.0", features = ["derive"] }
